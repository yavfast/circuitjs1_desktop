# Failing / Skipped elements (visual batch checks)

Дата: 2025-12-25

Цей список фіксує елементи, які **не проходять поточну серійну візуальну перевірку** (DevMode + `CircuitJS1.importFromJson()`), або які тимчасово **пропускаємо**, щоб не блокувати перевірку інших типів.

## Поточні проблеми

### Optocoupler
- Статус: **SKIP (blocked)**
- Симптом: `CircuitJS1.importFromJson()` не додає елемент (фактично `elementCount` не збільшується / елемент не з’являється).
- Контекст: проблема проявилась під час batch-імпорту (геометричний рефакторинг), відтворюється ізольованим імпортом типу `Optocoupler`.
- План: не блокуємо візуальне покриття решти елементів; повернутись після стабілізації/діагностики та окремо виправити JSON import/finalize/setPoints для `Optocoupler`.
